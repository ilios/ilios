To install necessary components to run Cucumber tests:

* Make sure you have Ruby, Vagrant, and VirtualBox installed.
* From this directory, type `bundle install`

To run Cucumber tests:

* From this directory, type `cucumber`

If your Vagrant-ized instance of Ilios is not already running on your local machine
then it may take a few minutes before the tests start.

For more information, see http://cukes.info/
