/ingestionexceptions:
      get:
        summary: Fetch all Ingestionexceptions
        description: |
          Ilios Ingestionexceptions
        parameters:
          - name: offset
            in: query
            description: Offset
            required: false
            type: number
            format: integer
          - name: limit
            in: query
            description: Limit Results
            required: false
            type: number
            format: integer
            default: 20
          - name: order_by
            in: query
            description: Order by fields.  Must by an array ie. &order_by[name]=ASC&order_by[description]=DESC
            required: false
            type: array
          - name: filters
            in: query
            description: Filter by fields. Must be an array ie. &filters[id]=3
            required: false
            type: array
        tags:
          - Ingestionexceptions
        responses:
          200:
            description: An array of ingestionexceptions
            schema:
              type: object
              properties:
                ingestionexceptions:
                  type: array
                  items:
                    $ref: '#/definitions/Ingestionexception'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
      post:
        summary: Create a Ingestionexceptions
        description: |
          Ilios Ingestionexceptions
        parameters:
        - name: body
          in: body
          required: true
          schema:
              type: object
              properties:
                ingestionexception:
                  $ref: '#/definitions/Ingestionexception'
        tags:
          - Ingestionexceptions
        responses:
          201:
            description: A single Ingestionexception
            schema:
              type: object
              properties:
                ingestionexceptions:
                  type: array
                  items:
                    $ref: '#/definitions/Ingestionexception'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
/ingestionexceptions/{id}:
  get:
    summary: Fetch a single Ingestionexception
    description: |
          Ilios Ingestionexception
    parameters:
    - name: id
      in: path
      required: true
      description: id
      type: integer
    tags:
      - Ingestionexceptions
    responses:
      200:
        description: A single Ingestionexception
        schema:
          type: object
          properties:
            ingestionexceptions:
              type: array
              items:
                $ref: '#/definitions/Ingestionexception'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  put:
    summary: Edit a Ingestionexception
    description: |
      Ilios Ingestionexceptions
    parameters:
    - name: id
      in: path
      required: true
      description: id
      type: integer
    - name: body
      in: body
      required: true
      schema:
          type: object
          properties:
            ingestionexception:
              $ref: '#/definitions/Ingestionexception'
    tags:
      - Ingestionexceptions
    responses:
      200:
        description: A single Ingestionexception
        schema:
          type: object
          properties:
            ingestionexception:
              $ref: '#/definitions/Ingestionexception'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  delete:
    summary: Delete Ingestionexception
    description: |
          Delete Ilios Ingestionexception
    parameters:
    - name: id
      in: path
      required: true
      description: id
      type: integer
    tags:
      - Ingestionexceptions
    responses:
      204:
        description: Deleted Ingestionexception
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'