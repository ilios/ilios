/ingestionexceptions:
  get:
    summary: Fetch all Ingestionexceptions
    parameters:
      - name: X-JWT-Authorization
        in: header
        description: API JWT Token
        required: true
        type: string
        default: Token XXX
      - name: offset
        in: query
        description: Offset
        required: false
        type: integer
      - name: limit
        in: query
        description: Limit Results
        required: false
        type: integer
        default: 20
      - name: order_by
        in: query
        description: Order by fields.  Must by an array ie. &order_by[name]=ASC&order_by[description]=DESC
        required: false
        type: array
      - name: filters
        in: query
        description: Filter by fields. Must be an array ie. &filters[id]=3
        required: false
        type: array
    tags:
      - Ingestionexceptions
    responses:
      200:
        description: An array of ingestionexceptions
        schema:
          type: object
          properties:
            ingestionexceptions:
              type: array
              items:
                $ref: '#/definitions/Ingestionexception'
  post:
    summary: Create Ingestionexceptions
    parameters:
    - name: X-JWT-Authorization
      in: header
      description: API JWT Token
      required: true
      type: string
      default: Token XXX
    - name: body
      in: body
      required: true
      schema:
          type: object
          properties:
            ingestionexceptions:
              type: array
              items:
                $ref: '#/definitions/Ingestionexception'
    tags:
      - Ingestionexceptions
    responses:
      201:
        description: An array of newly created ingestionexceptions
        schema:
          type: object
          properties:
            ingestionexceptions:
              type: array
              items:
                $ref: '#/definitions/Ingestionexception'
      400:
        description: Bad Request Data
      403:
        description: Access Denied
/ingestionexceptions/{id}:
  get:
    summary: Fetch a single Ingestionexception
    parameters:
    - name: X-JWT-Authorization
      in: header
      description: API JWT Token
      required: true
      type: string
      default: Token XXX
    - name: id
      in: path
      required: true
      description: id
      type: integer
    tags:
      - Ingestionexceptions
    responses:
      200:
        description: A single Ingestionexception
        schema:
          type: object
          properties:
            ingestionexceptions:
              type: array
              items:
                $ref: '#/definitions/Ingestionexception'
      404:
        description: Not found
  put:
    summary: Edit a Ingestionexception
    parameters:
    - name: X-JWT-Authorization
      in: header
      description: API JWT Token
      required: true
      type: string
      default: Token XXX
    - name: id
      in: path
      required: true
      description: id
      type: integer
    - name: body
      in: body
      required: true
      schema:
          type: object
          properties:
            ingestionexception:
              $ref: '#/definitions/Ingestionexception'
    tags:
      - Ingestionexceptions
    responses:
      200:
        description: A single Ingestionexception
        schema:
          type: object
          properties:
            ingestionexception:
              $ref: '#/definitions/Ingestionexception'
      400:
        description: Bad Request Data
      403:
        description: Access Denied
      404:
        description: Not Found
  delete:
    summary: Delete a Ingestionexception
    parameters:
    - name: X-JWT-Authorization
      in: header
      description: API JWT Token
      required: true
      type: string
      default: Token XXX
    - name: id
      in: path
      required: true
      description: id
      type: integer
    tags:
      - Ingestionexceptions
    responses:
      204:
        description: Deleted Ingestionexception
      404:
        description: Not Found
      500:
        description: Deletion not allowed