/learningmaterials:
      get:
        summary: Fetch all Learningmaterials
        description: |
          Ilios Learningmaterials
        parameters:
          - name: offset
            in: query
            description: Offset
            required: false
            type: number
            format: integer
          - name: limit
            in: query
            description: Limit Results
            required: false
            type: number
            format: integer
            default: 20
          - name: order_by
            in: query
            description: Order by fields.  Must by an array ie. &order_by[name]=ASC&order_by[description]=DESC
            required: false
            type: array
          - name: filters
            in: query
            description: Filter by fields. Must be an array ie. &filters[id]=3
            required: false
            type: array
        tags:
          - Learningmaterials
        responses:
          200:
            description: An array of learningmaterials
            schema:
              type: object
              properties:
                learningmaterials:
                  type: array
                  items:
                    $ref: '#/definitions/Learningmaterial'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
      post:
        summary: Create a Learningmaterials
        description: |
          Ilios Learningmaterials
        parameters:
        - name: body
          in: body
          required: true
          schema:
              type: object
              properties:
                learningmaterial:
                  $ref: '#/definitions/Learningmaterial'
        tags:
          - Learningmaterials
        responses:
          201:
            description: A single Learningmaterial
            schema:
              type: object
              properties:
                learningmaterials:
                  type: array
                  items:
                    $ref: '#/definitions/Learningmaterial'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
/learningmaterials/{id}:
  get:
    summary: Fetch a single Learningmaterial
    description: |
          Ilios Learningmaterial
    parameters:
    - name: id
      in: path
      required: true
      description: id
      type: integer
    tags:
      - Learningmaterials
    responses:
      200:
        description: A single Learningmaterial
        schema:
          type: object
          properties:
            learningmaterials:
              type: array
              items:
                $ref: '#/definitions/Learningmaterial'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  put:
    summary: Edit a Learningmaterial
    description: |
      Ilios Learningmaterials
    parameters:
    - name: id
      in: path
      required: true
      description: id
      type: integer
    - name: body
      in: body
      required: true
      schema:
          type: object
          properties:
            learningmaterial:
              $ref: '#/definitions/Learningmaterial'
    tags:
      - Learningmaterials
    responses:
      200:
        description: A single Learningmaterial
        schema:
          type: object
          properties:
            learningmaterial:
              $ref: '#/definitions/Learningmaterial'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  delete:
    summary: Delete Learningmaterial
    description: |
          Delete Ilios Learningmaterial
    tags:
      - Learningmaterials
    responses:
      204:
        description: Deleted Learningmaterial
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'