/sessiontypes:
      get:
        summary: Fetch all Sessiontypes
        description: |
          Ilios Sessiontypes
        parameters:
          - name: offset
            in: query
            description: Offset
            required: false
            type: number
            format: integer
          - name: limit
            in: query
            description: Limit Results
            required: false
            type: number
            format: integer
            default: 20
          - name: order_by
            in: query
            description: Order by fields.  Must by an array ie. &order_by[name]=ASC&order_by[description]=DESC
            required: false
            type: array
          - name: filters
            in: query
            description: Filter by fields. Must be an array ie. &filters[id]=3
            required: false
            type: array
        tags:
          - Sessiontypes
        responses:
          200:
            description: An array of sessiontypes
            schema:
              type: object
              properties:
                sessiontypes:
                  type: array
                  items:
                    $ref: '#/definitions/Sessiontype'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
      post:
        summary: Create a Sessiontypes
        description: |
          Ilios Sessiontypes
        parameters:
        - name: body
          in: body
          required: true
          schema:
              type: object
              properties:
                sessiontype:
                  $ref: '#/definitions/Sessiontype'
        tags:
          - Sessiontypes
        responses:
          201:
            description: A single Sessiontype
            schema:
              type: object
              properties:
                sessiontypes:
                  type: array
                  items:
                    $ref: '#/definitions/Sessiontype'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
/sessiontypes/{id}:
  get:
    summary: Fetch a single Sessiontype
    description: |
          Ilios Sessiontype
    parameters:
    - name: id
      in: path
      required: true
      description: id
      type: integer
    tags:
      - Sessiontypes
    responses:
      200:
        description: A single Sessiontype
        schema:
          type: object
          properties:
            sessiontypes:
              type: array
              items:
                $ref: '#/definitions/Sessiontype'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  put:
    summary: Edit a Sessiontype
    description: |
      Ilios Sessiontypes
    parameters:
    - name: id
      in: path
      required: true
      description: id
      type: integer
    - name: body
      in: body
      required: true
      schema:
          type: object
          properties:
            sessiontype:
              $ref: '#/definitions/Sessiontype'
    tags:
      - Sessiontypes
    responses:
      200:
        description: A single Sessiontype
        schema:
          type: object
          properties:
            sessiontype:
              $ref: '#/definitions/Sessiontype'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  delete:
    summary: Delete Sessiontype
    description: |
          Delete Ilios Sessiontype
    tags:
      - Sessiontypes
    responses:
      204:
        description: Deleted Sessiontype
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'