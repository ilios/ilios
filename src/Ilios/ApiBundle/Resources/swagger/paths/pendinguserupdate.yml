/pendinguserupdates:
      get:
        summary: Fetch all Pendinguserupdates
        description: |
          Ilios Pendinguserupdates
        parameters:
          - name: offset
            in: query
            description: Offset
            required: false
            type: number
            format: integer
          - name: limit
            in: query
            description: Limit Results
            required: false
            type: number
            format: integer
            default: 20
          - name: order_by
            in: query
            description: Order by fields.  Must by an array ie. &order_by[name]=ASC&order_by[description]=DESC
            required: false
            type: array
          - name: filters
            in: query
            description: Filter by fields. Must be an array ie. &filters[id]=3
            required: false
            type: array
        tags:
          - Pendinguserupdates
        responses:
          200:
            description: An array of pendinguserupdates
            schema:
              type: object
              properties:
                pendinguserupdates:
                  type: array
                  items:
                    $ref: '#/definitions/Pendinguserupdate'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
      post:
        summary: Create a Pendinguserupdates
        description: |
          Ilios Pendinguserupdates
        parameters:
        - name: body
          in: body
          required: true
          schema:
              type: object
              properties:
                pendinguserupdate:
                  $ref: '#/definitions/Pendinguserupdate'
        tags:
          - Pendinguserupdates
        responses:
          201:
            description: A single Pendinguserupdate
            schema:
              type: object
              properties:
                pendinguserupdates:
                  type: array
                  items:
                    $ref: '#/definitions/Pendinguserupdate'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
/pendinguserupdates/{id}:
  get:
    summary: Fetch a single Pendinguserupdate
    description: |
          Ilios Pendinguserupdate
    tags:
      - Pendinguserupdates
    responses:
      200:
        description: A single Pendinguserupdate
        schema:
          type: object
          properties:
            pendinguserupdates:
              type: array
              items:
                $ref: '#/definitions/Pendinguserupdate'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  put:
    summary: Edit a Pendinguserupdate
    description: |
      Ilios Pendinguserupdates
    parameters:
    - name: body
      in: body
      required: true
      schema:
          type: object
          properties:
            pendinguserupdate:
              $ref: '#/definitions/Pendinguserupdate'
    tags:
      - Pendinguserupdates
    responses:
      200:
        description: A single Pendinguserupdate
        schema:
          type: object
          properties:
            pendinguserupdate:
              $ref: '#/definitions/Pendinguserupdate'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  delete:
    summary: Delete Pendinguserupdate
    description: |
          Delete Ilios Pendinguserupdate
    tags:
      - Pendinguserupdates
    responses:
      204:
        description: Deleted Pendinguserupdate
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'