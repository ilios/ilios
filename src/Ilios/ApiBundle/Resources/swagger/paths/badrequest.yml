/badrequests:
  get:
    summary: Fetch all Badrequests
    parameters:
      - name: X-JWT-Authorization
        in: header
        description: API JWT Token
        required: true
        type: string
        default: Token XXX
      - name: offset
        in: query
        description: Offset
        required: false
        type: integer
      - name: limit
        in: query
        description: Limit Results
        required: false
        type: integer
        default: 20
      - name: order_by
        in: query
        description: Order by fields.  Must by an array ie. &order_by[name]=ASC&order_by[description]=DESC
        required: false
        type: array
      - name: filters
        in: query
        description: Filter by fields. Must be an array ie. &filters[id]=3
        required: false
        type: array
    tags:
      - Badrequests
    responses:
      200:
        description: An array of badrequests
        schema:
          type: object
          properties:
            badrequests:
              type: array
              items:
                $ref: '#/definitions/Badrequest'
  post:
    summary: Create Badrequests
    parameters:
    - name: X-JWT-Authorization
      in: header
      description: API JWT Token
      required: true
      type: string
      default: Token XXX
    - name: body
      in: body
      required: true
      schema:
          type: object
          properties:
            badrequests:
              type: array
              items:
                $ref: '#/definitions/Badrequest'
    tags:
      - Badrequests
    responses:
      201:
        description: An array of newly created badrequests
        schema:
          type: object
          properties:
            badrequests:
              type: array
              items:
                $ref: '#/definitions/Badrequest'
      400:
        description: Bad Request Data
      403:
        description: Access Denied
/badrequests/{id}:
  get:
    summary: Fetch a single Badrequest
    parameters:
    - name: X-JWT-Authorization
      in: header
      description: API JWT Token
      required: true
      type: string
      default: Token XXX
    - name: id
      in: path
      required: true
      description: id
      type: integer
    tags:
      - Badrequests
    responses:
      200:
        description: A single Badrequest
        schema:
          type: object
          properties:
            badrequests:
              type: array
              items:
                $ref: '#/definitions/Badrequest'
      404:
        description: Not found
  put:
    summary: Edit a Badrequest
    parameters:
    - name: X-JWT-Authorization
      in: header
      description: API JWT Token
      required: true
      type: string
      default: Token XXX
    - name: id
      in: path
      required: true
      description: id
      type: integer
    - name: body
      in: body
      required: true
      schema:
          type: object
          properties:
            badrequest:
              $ref: '#/definitions/Badrequest'
    tags:
      - Badrequests
    responses:
      200:
        description: A single Badrequest
        schema:
          type: object
          properties:
            badrequest:
              $ref: '#/definitions/Badrequest'
      400:
        description: Bad Request Data
      403:
        description: Access Denied
      404:
        description: Not Found
  delete:
    summary: Delete a Badrequest
    parameters:
    - name: X-JWT-Authorization
      in: header
      description: API JWT Token
      required: true
      type: string
      default: Token XXX
    - name: id
      in: path
      required: true
      description: id
      type: integer
    tags:
      - Badrequests
    responses:
      204:
        description: Deleted Badrequest
      404:
        description: Not Found
      500:
        description: Deletion not allowed