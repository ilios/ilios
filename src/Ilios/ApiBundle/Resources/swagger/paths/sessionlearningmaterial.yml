/sessionlearningmaterials:
      get:
        summary: Fetch all Sessionlearningmaterials
        description: |
          Ilios Sessionlearningmaterials
        parameters:
          - name: offset
            in: query
            description: Offset
            required: false
            type: number
            format: integer
          - name: limit
            in: query
            description: Limit Results
            required: false
            type: number
            format: integer
            default: 20
          - name: order_by
            in: query
            description: Order by fields.  Must by an array ie. &order_by[name]=ASC&order_by[description]=DESC
            required: false
            type: array
          - name: filters
            in: query
            description: Filter by fields. Must be an array ie. &filters[id]=3
            required: false
            type: array
        tags:
          - Sessionlearningmaterials
        responses:
          200:
            description: An array of sessionlearningmaterials
            schema:
              type: object
              properties:
                sessionlearningmaterials:
                  type: array
                  items:
                    $ref: '#/definitions/Sessionlearningmaterial'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
      post:
        summary: Create a Sessionlearningmaterials
        description: |
          Ilios Sessionlearningmaterials
        parameters:
        - name: body
          in: body
          required: true
          schema:
              type: object
              properties:
                sessionlearningmaterial:
                  $ref: '#/definitions/Sessionlearningmaterial'
        tags:
          - Sessionlearningmaterials
        responses:
          201:
            description: A single Sessionlearningmaterial
            schema:
              type: object
              properties:
                sessionlearningmaterials:
                  type: array
                  items:
                    $ref: '#/definitions/Sessionlearningmaterial'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
/sessionlearningmaterials/{id}:
  get:
    summary: Fetch a single Sessionlearningmaterial
    description: |
          Ilios Sessionlearningmaterial
    parameters:
    - name: id
      in: path
      required: true
      description: id
      type: integer
    tags:
      - Sessionlearningmaterials
    responses:
      200:
        description: A single Sessionlearningmaterial
        schema:
          type: object
          properties:
            sessionlearningmaterials:
              type: array
              items:
                $ref: '#/definitions/Sessionlearningmaterial'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  put:
    summary: Edit a Sessionlearningmaterial
    description: |
      Ilios Sessionlearningmaterials
    parameters:
    - name: id
      in: path
      required: true
      description: id
      type: integer
    - name: body
      in: body
      required: true
      schema:
          type: object
          properties:
            sessionlearningmaterial:
              $ref: '#/definitions/Sessionlearningmaterial'
    tags:
      - Sessionlearningmaterials
    responses:
      200:
        description: A single Sessionlearningmaterial
        schema:
          type: object
          properties:
            sessionlearningmaterial:
              $ref: '#/definitions/Sessionlearningmaterial'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  delete:
    summary: Delete Sessionlearningmaterial
    description: |
          Delete Ilios Sessionlearningmaterial
    tags:
      - Sessionlearningmaterials
    responses:
      204:
        description: Deleted Sessionlearningmaterial
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'