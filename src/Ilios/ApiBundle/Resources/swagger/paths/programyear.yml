/programyears:
      get:
        summary: Fetch all Programyears
        description: |
          Ilios Programyears
        parameters:
          - name: offset
            in: query
            description: Offset
            required: false
            type: number
            format: integer
          - name: limit
            in: query
            description: Limit Results
            required: false
            type: number
            format: integer
            default: 20
          - name: order_by
            in: query
            description: Order by fields.  Must by an array ie. &order_by[name]=ASC&order_by[description]=DESC
            required: false
            type: array
          - name: filters
            in: query
            description: Filter by fields. Must be an array ie. &filters[id]=3
            required: false
            type: array
        tags:
          - Programyears
        responses:
          200:
            description: An array of programyears
            schema:
              type: object
              properties:
                programyears:
                  type: array
                  items:
                    $ref: '#/definitions/Programyear'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
      post:
        summary: Create a Programyears
        description: |
          Ilios Programyears
        parameters:
        - name: body
          in: body
          required: true
          schema:
              type: object
              properties:
                programyear:
                  $ref: '#/definitions/Programyear'
        tags:
          - Programyears
        responses:
          201:
            description: A single Programyear
            schema:
              type: object
              properties:
                programyears:
                  type: array
                  items:
                    $ref: '#/definitions/Programyear'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
/programyears/{id}:
  get:
    summary: Fetch a single Programyear
    description: |
          Ilios Programyear
    tags:
      - Programyears
    responses:
      200:
        description: A single Programyear
        schema:
          type: object
          properties:
            programyears:
              type: array
              items:
                $ref: '#/definitions/Programyear'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  put:
    summary: Edit a Programyear
    description: |
      Ilios Programyears
    parameters:
    - name: body
      in: body
      required: true
      schema:
          type: object
          properties:
            programyear:
              $ref: '#/definitions/Programyear'
    tags:
      - Programyears
    responses:
      200:
        description: A single Programyear
        schema:
          type: object
          properties:
            programyear:
              $ref: '#/definitions/Programyear'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  delete:
    summary: Delete Programyear
    description: |
          Delete Ilios Programyear
    tags:
      - Programyears
    responses:
      204:
        description: Deleted Programyear
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'