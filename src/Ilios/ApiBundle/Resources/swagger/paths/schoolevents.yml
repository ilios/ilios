/schoolevents:
      get:
        summary: Fetch all Schoolevents
        description: |
          Ilios Schoolevents
        parameters:
          - name: offset
            in: query
            description: Offset
            required: false
            type: number
            format: integer
          - name: limit
            in: query
            description: Limit Results
            required: false
            type: number
            format: integer
            default: 20
          - name: order_by
            in: query
            description: Order by fields.  Must by an array ie. &order_by[name]=ASC&order_by[description]=DESC
            required: false
            type: array
          - name: filters
            in: query
            description: Filter by fields. Must be an array ie. &filters[id]=3
            required: false
            type: array
        tags:
          - Schoolevents
        responses:
          200:
            description: An array of schoolevents
            schema:
              type: object
              properties:
                schoolevents:
                  type: array
                  items:
                    $ref: '#/definitions/Schoolevent'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
      post:
        summary: Create a Schoolevents
        description: |
          Ilios Schoolevents
        parameters:
        - name: body
          in: body
          required: true
          schema:
              type: object
              properties:
                schoolevent:
                  $ref: '#/definitions/Schoolevent'
        tags:
          - Schoolevents
        responses:
          201:
            description: A single Schoolevent
            schema:
              type: object
              properties:
                schoolevents:
                  type: array
                  items:
                    $ref: '#/definitions/Schoolevent'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
/schoolevents/{id}:
  get:
    summary: Fetch a single Schoolevent
    description: |
          Ilios Schoolevent
    parameters:
    - name: id
      in: path
      required: true
      description: id
      type: integer
    tags:
      - Schoolevents
    responses:
      200:
        description: A single Schoolevent
        schema:
          type: object
          properties:
            schoolevents:
              type: array
              items:
                $ref: '#/definitions/Schoolevent'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  put:
    summary: Edit a Schoolevent
    description: |
      Ilios Schoolevents
    parameters:
    - name: id
      in: path
      required: true
      description: id
      type: integer
    - name: body
      in: body
      required: true
      schema:
          type: object
          properties:
            schoolevent:
              $ref: '#/definitions/Schoolevent'
    tags:
      - Schoolevents
    responses:
      200:
        description: A single Schoolevent
        schema:
          type: object
          properties:
            schoolevent:
              $ref: '#/definitions/Schoolevent'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  delete:
    summary: Delete Schoolevent
    description: |
          Delete Ilios Schoolevent
    parameters:
    - name: id
      in: path
      required: true
      description: id
      type: integer
    tags:
      - Schoolevents
    responses:
      204:
        description: Deleted Schoolevent
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'