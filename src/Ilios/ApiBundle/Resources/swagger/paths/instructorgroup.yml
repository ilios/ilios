/instructorgroups:
      get:
        summary: Fetch all Instructorgroups
        description: |
          Ilios Instructorgroups
        parameters:
          - name: offset
            in: query
            description: Offset
            required: false
            type: number
            format: integer
          - name: limit
            in: query
            description: Limit Results
            required: false
            type: number
            format: integer
            default: 20
          - name: order_by
            in: query
            description: Order by fields.  Must by an array ie. &order_by[name]=ASC&order_by[description]=DESC
            required: false
            type: array
          - name: filters
            in: query
            description: Filter by fields. Must be an array ie. &filters[id]=3
            required: false
            type: array
        tags:
          - Instructorgroups
        responses:
          200:
            description: An array of instructorgroups
            schema:
              type: object
              properties:
                instructorgroups:
                  type: array
                  items:
                    $ref: '#/definitions/Instructorgroup'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
      post:
        summary: Create a Instructorgroups
        description: |
          Ilios Instructorgroups
        parameters:
        - name: body
          in: body
          required: true
          schema:
              type: object
              properties:
                instructorgroup:
                  $ref: '#/definitions/Instructorgroup'
        tags:
          - Instructorgroups
        responses:
          201:
            description: A single Instructorgroup
            schema:
              type: object
              properties:
                instructorgroups:
                  type: array
                  items:
                    $ref: '#/definitions/Instructorgroup'
          default:
            description: Unexpected error
            schema:
              $ref: '#/definitions/Error'
/instructorgroups/{id}:
  get:
    summary: Fetch a single Instructorgroup
    description: |
          Ilios Instructorgroup
    tags:
      - Instructorgroups
    responses:
      200:
        description: A single Instructorgroup
        schema:
          type: object
          properties:
            instructorgroups:
              type: array
              items:
                $ref: '#/definitions/Instructorgroup'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  put:
    summary: Edit a Instructorgroup
    description: |
      Ilios Instructorgroups
    parameters:
    - name: body
      in: body
      required: true
      schema:
          type: object
          properties:
            instructorgroup:
              $ref: '#/definitions/Instructorgroup'
    tags:
      - Instructorgroups
    responses:
      200:
        description: A single Instructorgroup
        schema:
          type: object
          properties:
            instructorgroup:
              $ref: '#/definitions/Instructorgroup'
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'
  delete:
    summary: Delete Instructorgroup
    description: |
          Delete Ilios Instructorgroup
    tags:
      - Instructorgroups
    responses:
      204:
        description: Deleted Instructorgroup
      default:
        description: Unexpected error
        schema:
          $ref: '#/definitions/Error'