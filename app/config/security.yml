security:
    firewalls:
        api_docs:
            pattern:   ^/api/doc
            anonymous: ~
        default:
            pattern:    ^/api
            stateless: true
            jwt: ~
            provider: ilios_user_entity

    providers:
        ilios_user_entity:
            entity:
                class: IliosCoreBundle:User
                property: id
