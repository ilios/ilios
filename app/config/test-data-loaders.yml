services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true
        autoconfigure: true
        public: true

    Tests\AppBundle\DataLoader\:
        resource: '%kernel.project_dir%/tests/AppBundle/DataLoader'

    Tests\AppBundle\Fixture\:
        resource: '%kernel.project_dir%/tests/AppBundle/Fixture'