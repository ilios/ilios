---
:logger: puppet

:backends:
    - yaml

:yaml:
    :datadir: '/etc/puppetlabs/code/environments'

:hierarchy:
    - '%{environment}/hiera/%{fqdn}'
    - '%{environment}/hiera/common'
    - common
