<h1>
    {{group.id}}:
    {{group.title}}
</h1>
<h5>Users</h5>
<ul>
    {% for userId in group.users %}
    <li>
        <a href="{{ path('get_user', {'id': userId, '_format': 'html'}) }}">
            {{ userId }}
        </a>
    </li>
    {% endfor %}
</ul>

<h5>Offerings</h5>
<ul>
    {% for offeringId in group.offerings %}
    <li>
        <a href="{{ path('get_offering', {'id': offeringId, '_format': 'html'}) }}">
            {{ offeringId }}
        </a>
    </li>
    {% endfor %}
</ul>
