<section class='fullwidth'>
    <h4>Events for week {{week}} in {{year}}</h4>
    <ol>
        {{#each calendarEvents}}
                <li>Event: {{title}} {{start}} - {{end}}</li>
        {{/each}}
    </ol>
</section>
